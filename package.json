{
	"author": "Dimas López <me@dimaslz.io> (dimaslz)",
  "repository": {
    "url": "git+https://github.com/dimaslz/react-dynamic-classname.git",
    "type": "git"
  },
  "private": true,
  "workspaces": ["playground", "./packages/*"],
	"scripts": {
		"test": "yarn lib:test",
		"lint": "yarn lint:lib && yarn playground:lint",
		"playground": "yarn workspace playground",
		"preplayground:build": "yarn lib:build",
		"playground:build": "yarn playground build",
		"playground:dev": "yarn playground dev",
		"playground:lint": "yarn playground lint",
		"lib": "yarn workspace @dimaslz/react-dynamic-classname",
		"lib:test": "yarn lib test",
		"lib:build": "yarn lib build",
		"lib:lint": "yarn lib lint"
	}
}